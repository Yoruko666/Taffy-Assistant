# Mock 家具端运行依赖
# 安装：pip install -r furniture/requirements.txt

websockets>=11.0      # WebSocket 客户端
webrtcvad>=2.0.10     # 端点检测 VAD（端侧自动断句）
numpy>=1.24           # 数组处理
sounddevice>=0.4.6    # 跨平台实时麦克风采集（Windows / macOS / Linux）
paho-mqtt>=2.0        # MQTT 客户端（设备模拟器）
